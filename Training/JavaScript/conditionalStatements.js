let age = 10
let fname = 'Harry'

if (age > 18) {
    console.log(`${fname} is eligible for Driving Licence`);
} else {
    console.log(`${fname}, your age is ${age} You have to wait for ${18-age} to apply for DL`);
}


